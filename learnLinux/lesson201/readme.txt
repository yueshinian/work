进程：程序分配的基本单位
单道程序：一个程序运行。
多道程序：一个程序。
时间片：操作系统分配给每个正在运行的进程的时间。linux，5-800ms
todo：进程调度策略。
并行：指在同一时刻，有多条指令在多个处理器运行。
并发：多个进程轮换执行。
PCB：进程控制块。为每一个进程分配，维护进程相关信息。内核是task_struct结构体。id，状态，切换时上下文（寄存器），
        虚拟地址空间，控制终端的信息，当前工作目录，umask，文件描述表，信号，用户id，信号id，会话，资源上限（ulimit）。